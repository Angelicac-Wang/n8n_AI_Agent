{
  "nodeName": "gitlab",
  "summary": "此節點可從 GitLab API 擷取資料。",
  "parameters": [
    {
      "name": "authentication",
      "type": "options",
      "description": "指定用於驗證的方法，可以是存取權杖或 OAuth2。"
    },
    {
      "name": "resource",
      "type": "options",
      "description": "指定要操作的資源類型，如檔案、問題、發布、存儲庫、使用者或自訂 API 呼叫。"
    },
    {
      "name": "operation",
      "type": "options",
      "description": "指定要執行的操作，根據所選資源不同，操作選項也會有所變化。"
    }
  ],
  "example": {
    "node": {
      "parameters": {
        "authentication": "accessToken",
        "resource": "issue",
        "operation": "create"
      }
    }
  }
}